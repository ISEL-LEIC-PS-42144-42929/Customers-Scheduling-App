<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.ps.isel.customersscheduling">

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.AppCompat.Light.NoActionBar">
    <activity android:name=".Activities.MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".Activities.BusinessRegistrationActivity" />
    <activity android:name=".Activities.BusinessScheduleActivity">

    </activity>
    <activity android:name=".Activities.FilterActivity" />
    <activity android:name=".Activities.SearchResultsActivity" />
    <activity android:name=".Activities.DefinitionsActivity" />
    <activity android:name=".Activities.FavouritesActivity" />
    <activity android:name=".Activities.RegisterBusinessActivity" />
    <activity android:name=".Activities.PendentRequestsActivity"></activity>
</application>

</manifest>



